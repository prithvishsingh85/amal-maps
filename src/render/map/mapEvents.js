export const onZoomChangeEvent = (map) => {
  // This callback function will be called whenever the zoom level changes.
  // You can add your custom logic here.
  // console.log('Zoom level changed to:', map.getZoom());
};
